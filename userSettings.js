const userSettings = {
  chosenFields: [
    "location",
    "mode",
    "map",
    "player_count",
    "playgroup",
    "balance"
  ],
  filters: {
    rankedOnly: true,
    openServersOnly: true,
  },
  sortBy: {
    field: "player_count",
    order: "asc",
  }
};

export default userSettings;
